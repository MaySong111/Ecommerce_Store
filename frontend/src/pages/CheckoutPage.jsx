import { Typography } from "@mui/material";

export default function CheckoutPage() {
  return (
    <Typography variant="h3">
      Checkout Page, if you can see this, then you are authenticated.
    </Typography>
  );
}
